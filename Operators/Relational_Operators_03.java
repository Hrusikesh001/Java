import java.util.Scanner;

public class Relational_Operators_03 {
    public static void main(String[] args) {
        Scanner scanner = new Scanner(System.in);
        // int A = scanner.nextInt();
        // int B = scanner.nextInt();
        // System.out.println("Is A equal to B? " + (A == B));

        // int X = scanner.nextInt();
        // int Y = scanner.nextInt();
        // System.out.println("Is X not equal to Y? " + (X != Y));

        int M = scanner.nextInt();
        int N = scanner.nextInt();
        System.out.println("Is M greater than N? " + (M > N));
        System.out.println("Is M less than N? " + (M < N));
        System.out.println("Is M greater than or equal to N? " + (M >= N));
        System.out.println("Is M less than or equal to N? " + (M <= N));
    }
}
